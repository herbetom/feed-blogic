# Modules /lib/preinit/01_glinet-panel-boot loads before it starts the splash. pwm_bl
# comes first because the panel defers its probe on the backlight phandle.
GLINET_PANEL_MODULES="pwm_bl panel-mipi-dbi"
